/**
 * Feature Flags
 *
 * Central toggle for features that are under active development.
 * Flip a flag to `true` to make the feature visible across the app.
 */

/** When `true`, Study Mode cards, routes, and entry points are visible to users. */
export const FEATURE_STUDY_MODE = false;
