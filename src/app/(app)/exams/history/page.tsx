import { ComingSoonPage } from "@/features/dashboard/components/coming-soon-page";

export default function ExamHistoryPage() {
  return (
    <ComingSoonPage
      eyebrow="Exams"
      title="Exam history detail is coming soon"
      description="This area will expand into a fuller review surface for attempts, scores, timing, and answer breakdowns once the supporting pages are finished."
      primaryHref="/dashboard"
      primaryLabel="Return to dashboard"
    />
  );
}
